export enum ROUTE {
  HOME = "/",
  SEARCH = "/search",
  FAVORITES = "/favorites",
  SIGN_IN = "/sign-in",
  SIGN_UP = "/sign-up",
  ARTICLE = "/article/:id",
  BLOG = "/blog/:id",
  NOT_FOUND = "*",
  ACCOUNT = "/account",
  RESET = "/reset",
}
