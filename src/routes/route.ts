export enum ROUTE {
  HOME = "/",
  SEARCH = "/search",
  FAVORITES = "/favorites",
  SIGN_IN = "/sign-in",
  SIGN_UP = "/sign-up",
  CONTENT = "/article/:id",
  NOT_FOUND = "*",
  ACCOUNT = "/account",
}
